"""GSD X -- the applicability predicate gets an event.

Power Pack owns an evidence-conditioned applicability engine
(`modules.capability_runtime.applicability`). It is reachable only when an
operator types `/capability`, which makes it invocation-DEPENDENT: an applicable
engineering capability that requires the operator to remember its name.

Meanwhile a constant tier ladder already reaches the model on every prompt
(`~/.claude/hooks/power-pack-reminder.js:34`) and the model places itself on it.

This package is the join, and nothing more. It adds no registry, no state
authority, no vocabulary and no daemon.
"""
