"""frontier_intelligence -- FIOS (Frontier Intelligence Operating System) engines.

The EXECUTION layer of the Frontier Intelligence thesis: where the FD / CO / PM /
GK dataset families are KNOWLEDGE (the rules), FIOS is the live engine that RUNS
them. It invents no new doctrine and stands up no parallel metric -- every engine
here COMPOSES an existing, verified module surface:

  session_compiler  -- FIOS I-1 (+V-1/V-2 merged). Compiles a session DECLARATION
                       (objective/constraints/unknowns/candidate questions) into an
                       executable session plan (the 9-component "Session Zero"),
                       ranking questions through the FD-00 admission gate and the
                       FD-07 deposits-ledger floor. FD-00 is the protocol; this is
                       the executable that walks it.
  token_irr         -- FIOS III-2 (+III-3 merged). The frontier token as R&D
                       capital: immediate ROI, reuse multiplier, compound ROI,
                       payback, and a Frontier Dependence Index -- numeric, fed to
                       CO-12 (never a parallel accountant).
  evolution_engine  -- FIOS IV-1. Proposes mutations to EXISTING datasets
                       (compress/split/merge/abstract/specialize/deprecate/reinforce)
                       and NEVER auto-applies -- Owner-gated (T-FIOS-EVOLUTION-LOCK-001).

Central law (PR-FRONTIER-AS-RD-001): a frontier session is an R&D operation, not a
query. Every token must produce a permanent asset that lowers the need for future
frontier tokens. Everything here is fail-open: an engine error never blocks work.
"""
