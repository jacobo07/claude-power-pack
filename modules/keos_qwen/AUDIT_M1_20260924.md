# Auditoría Fase 4 — KEOS-Qwen M1 «CORPUS CLOSED»

**Fecha:** 2026-09-24 · **Auditor:** `oneshot-architect-auditor` (ONESHOT Fase 4) · **Gaps:** 25 (tope alcanzado, NO agotamiento)

> Persistido por el hilo principal. El auditor corrió con `Write` deshabilitado y **se negó a rodear
> la puerta por PowerShell** — *"una puerta que rechaza no es permiso"*. Conducta correcta; el
> informe es suyo, esta transcripción es mía.

## Instrumentos declarados (para que un cero sea interpretable)

- `Glob modules/*` → `modules/keos_qwen/` **no existe**. M1 es greenfield: ninguna colisión de ruta es
  posible. Todas las colisiones reportadas son **de propiedad**, no de fichero.
- `Grep` de taxonomía 0-6 sobre `claude-power-pack` y `cpp-goal-spine-wt` → **cero aciertos**. Barrido
  **por vocabulario**, así que la ausencia es débil; reportado como *el plan no cita la ruta del
  titular* (falsable), no como *no existe*.
- Lectura directa de `gsd_x/goal/providers/gate.py`, `providers/codex.py`, `goal/contract.py` —
  evidencia directa, sostiene los gaps 6, 7 y 15.
- `Grep tools/test_*` → 12/12 muestreados viven en el **top-level** `claude-power-pack/tools/`.

## Los 25 gaps

| # | clase | qué falta | por qué importa |
|---|---|---|---|
| 1 | AUTH | Ni clave SSH, ni `usuario@host`, ni política `known_hosts` para 5.9.23.174 | El router global declara `kobicraft_gex44` sólo para dominios KobiiCraft y cierra con *"Other VPS hosts: declare per-project before SSH"*. Sin declaración, el primer ssh resuelve a `id_ed25519`, muere en `Permission denied` y **ese fallo entra al corpus como si fuera del modelo**. Host nuevo ⇒ prompt de host key ⇒ ssh bloqueado en stdin ⇒ el cuelgue de puente documentado |
| 2 | AUTH | El endpoint es loopback y el plan no dice cómo lo cruza | `ssh -L` y `ssh host 'curl'` **no son equivalentes bajo HR-04**: el segundo ejecuta shell remoto, deja historial, y cualquier `>` es escritura viva |
| 3 | REALITY | «CERO escrituras a GEX44» es intención, no mecanismo — y es falsa en su borde | Una petición de inferencia muta el host: journald de `keos-llm` crece, VRAM/KV cambian. Disco al 93 % ⇒ el instrumento consume el recurso que mide |
| 4 | EDGE | Sin tercer desenlace: endpoint caído entra como fallo del modelo | `ConnectionRefused`, 504, OOM de GPU y `systemctl restart` producen todos "no hubo respuesta útil". La línea base acabaría midiendo la salud del host. El patrón exit 2 = INCONCLUSIVE **ya existe** en `kme_landmark_ingame_gate.py` |
| 5 | EDGE | Truncamiento por ctx 32768 sin campo ni desenlace | `finish_reason: "length"` produce un test a medias, sintácticamente inválido, que se leería como incompetencia del modelo cuando es una ventana que elegimos nosotros |
| 6 | INTEGRATION | W1 reconstruye el contrato de proveedor que Goal Spine ya tiene probado | `codex.py` ya implementa `dispatch/observe/harvest/cancel/probe` con wall-bound, marcador **antes** del spawn (L119-123, distingue "nunca arrancó" de "perdimos el handle"), matanza del árbol y `Observation(ENDED\|RUNNING\|LOST)`. `gate.py` añade que `poll()` desde proceso nuevo sólo puede decir LOST (L54-63) |
| 7 | INTEGRATION | Ignora la doctrina de CUENTA COMPARTIDA que `codex.py` ya pagó | `keos-llm` es recurso único, ~3,5 GB VRAM libre, 15 servicios de producción. `codex.py` resolvió esta forma exacta: kill-switch env **y** fichero-bandera, presupuesto diario desde ledger, cooldown compartido, lock con detección de titular muerto. El plan no tiene ninguno de los cuatro |
| 8 | GATE | El alias `qwen3-coder-30b` no es identidad | Un `.gguf` recuantizado, un llama.cpp nuevo o un `-ngl` distinto responden bajo el mismo alias. Cuando la regresión se ponga roja, nadie podrá decir si regresó el modelo o cambió el servidor |
| 9 | GATE | El sujeto es estocástico y el plan trata M1 como reproducible | Ningún fichero nombra `temperature`, `top_p`, `seed` ni nº de muestras. Aviso en contrario: `temp=0` **tampoco** da bit-determinismo en llama.cpp si cambian lote o slots |
| 10 | INTEGRATION | «EXTIENDE las CLASES 0-6» no cita dónde vive el titular | Única afirmación de reutilización del plan y la que no se puede comprobar. Una copia es correcta el día que se escribe y falsa el día que cualquiera de las dos cambia |
| 11 | PATH | Los tests aterrizan donde nada los recoge | `modules/keos_qwen/tools/test_*.py` no coincide con ningún patrón existente. Clase "el guardia nunca corrió": cada rama roja pasa contra un test que nada invoca |
| 12 | PATH | Faltan los `__init__.py` — **y la semilla del corpus es exactamente ese bug** | Los 28 ficheros no incluyen uno solo, así que el paquete no importa. El fallo de Qwen que siembra este corpus fue `import judge` en vez de `modules.gsd_x.goal.judge`. **El plan reproduce el defecto que viene a catalogar** |
| 13 | ENV | Intérprete y encoding sin declarar, con modo de fallo destructivo | `gate.py` L129 y `codex.py` L246 fijan **ambos** `PYTHONIOENCODING=utf-8`. Sin él, `open(path,"w")` **trunca antes de codificar** y un `UnicodeEncodeError` deja el fichero en 0 bytes |
| 14 | EDGE | Sobre a medio escribir, sin atomicidad ni lector que rechace parciales | Un ctrl-C o el disco al 93 % dejan JSON truncado; un `json.loads` en `try` que falla abierto lo convierte en registro vacío indistinguible de captura legítima |
| 15 | REALITY | El verificador del redactor comparte punto ciego con el redactor | Vacuna medida en este estate: *"subí una contraseña viva y el gate dio PASS"* porque el verificador re-ejecutaba la regex del redactor |
| 16 | EDGE | Dos superficies de secreto, sumidero heredado sin nombrar | El secreto del **prompt** lo construimos nosotros; el de la **salida** es adversarial. Y `codex.py` L313 vuelca `narrative=text.strip()[-2000:]` **sin redactar** al recibo |
| 17 | GATE | CLASE 7 no es falsable sin restricciones comprobables a máquina | "no mockees subprocess" por grep de `mock` hereda la Exención de Log Analítico: un comentario que explica por qué NO mockea se marca como violación. Sin resolverlo, CLASE 7 no distingue desobediencia de explicación |
| 18 | GATE | Ningún verificador declara control positivo — los tres pueden rechazar siempre | Trampa específica de `verify_executable.py`: si el entorno está mal (gap 12), **todo** código generado falla al importar y el gate mide su propio entorno |
| 19 | GATE | C2 no nombra qué modelo juzga | Qwen juzgando a Qwen está anclado; otro modelo mete un sujeto no pinado. En ambos casos C2 es probabilístico dentro de un hito cuyo valor es ser determinista |
| 20 | EDGE | `split.py` sin unidad de partición, casi-duplicados ni procedencia de semilla | (a) Particionar el **grupo de contaminación**, no la fila — hermanos a lado y lado y **nada aguas abajo puede detectarlo**; (b) pertenencia por ubicación, identidad por contenido; (c) semilla CSPRNG, nunca del llamador — si es argumento, se muele offline en segundos |
| 21 | GATE | Orden inválido entre línea base y conjunto congelado | Si la base se mide sobre el corpus completo y el congelado sale de ahí, la primera medición ya vio el reservado. El orden es físico: GOBERNAR → REGISTRAR → PARTICIONAR → CONGELAR → medir |
| 22 | INTEGRATION | `admission.py` puede minar en paralelo a `session_delta` + `owner_queue` | Dos mineros sobre el mismo material son dos verdades sobre qué candidatos existen |
| 23 | GATE | El drill debe mutar el **enlace**, no los extremos | Mutar `verify_executable.py` re-demuestra el verificador; no demuestra que `classify.py` lo llame. El valor de M1 es *quién decide la clase* |
| 24 | ANTI-CRASH | 28 ficheros sin puntos de control, en el host que menos lo tolera | Techo del estate: >10 ficheros ⇒ descomponer; batch seguro ≤5. Es el perfil exacto de la pantalla muerta cross-repo |
| 25 | PATH | `KEOS_QWEN.md` documentará comandos que nada ejecuta | Con los gaps 11 y 12 encima, la línea de validación del README falla el primer día y se lee como "lo estaré invocando mal". Riesgo específico: escribir que el gate impone X **antes** de que X exista |

## Items limpios (informativos)

- **Ninguna colisión de ruta.** Los 41 ficheros son greenfield.
- **La descomposición en 8 workstreams es correcta en su forma.** Los defectos están en los desenlaces
  que faltan dentro de cada capa, no en el orden.
- **La lectura de la carta K-ADOS es correcta.** *"a daemon"* bloquea el residente, no el ejecutor
  invocado; M1 no propone residente.
- **La semilla del corpus está bien elegida.** Trae las tres clases de interés *y* un acierto difícil
  (discriminó la mutación por la RAZÓN) — o sea, **su propio control positivo**.

## Nota de saturación (vinculante para futuros pases)

25 = **tope declarado, no agotamiento**. Tres frentes sin auditar: (a) solape entre `corpus/positive.py`
y `corpus/negative.py` con `taxonomy/`; (b) ciclo de vida de `registry/store.py` — quién es el escritor
legítimo y si es apendable; (c) si `c1/constraints.py` y `taxonomy/classes.py` acaban siendo dos
vocabularios para la misma cosa.

**Advertencia sobre un arreglo concreto:** los gaps 4 y 18 se arreglan con el **mismo** mecanismo (un
tercer desenlace que no es PASS ni FAIL). Arreglarlos por separado produciría dos vocabularios de
INCONCLUSIVE en un módulo cuya tesis es que las clases de fallo sean una sola.

## Corrección del hilo principal (T-KEOSQ-00)

El gap 3 alcanza a quien escribió el plan. Las sondas de esta sesión escribieron `/tmp/qp.json`,
`/tmp/qr.json`, `/tmp/kq_ctl.json`, `/tmp/kq_trt.json` y `/tmp/kq_ab.py` en GEX44 **mientras el plan
declaraba «cero escrituras» y con HR-04 sin satisfacer**. Benignos, y la afirmación era falsa.

**Lección:** el que declara el invariante es el primero que lo rompe si no lo cablea. «Cero escrituras»
sólo es verdad cuando `ssh_llama.py` rechaza por construcción todo argv con comando remoto.
