# CPP-ACI — HANDOFF (continuity across sessions)

> A fresh session reads THIS file, then `BUILD_STATUS.md`, then `MASTER_BUILD_PLAN.md`, then executes. Do not reconstruct prior reasoning.

## Identity
- **Mission:** CPP Autopoietic Cognitive Institution (CPP-ACI) — dataset family build.
- **Output dir:** `C:\Users\User\Downloads\! Promptsss\Datasets Claude Power Pack\CPP Autopoietic Cognitive Institution\`
- **Thesis:** turn PP's accumulated knowledge into executable, verifiable, transferable, compounding institutional capability — as a *corpus* (not production code this mission).

## Exact state (2026-07-12)
- Phase -1 (Reality Scan Forense): **COMPLETE**. Source spec read to EOF (3,282 lines); PP estate inventoried (18 KB domains ~524k words + 70+ built modules); CUAE benchmark profiled; MACRO_AUDIT read.
- **BLOCKED AT STOP #1.** The System Redundancy Tribunal found that the full 12-science / 15-strata / 36-system spec would duplicate ~400–500k words of already-SEALED PP work. Recommended architecture is the **circulatory delta** (~6–8 datasets). Owner must choose the architecture before ANY dataset is built.

## The decision the Owner must make (STOP #1)
Two paths, posed as a question in-session:
- **Path A (recommended):** build only the genuine delta — Foundations (F1–F3) + Institutional Circulatory Fabric (C1) + Cross-Project Federation (C2) + Meta-Institutional Control Plane (M1), with a NON_DUPLICATION_LEDGER routing the 12 sciences to their existing owners. ~6–8 datasets.
- **Path B:** build the full conceptual compendium anyway (deliberate overlap accepted, as a teaching reference distinct from the runtime). ~15–25 datasets, requires explicit override of `PR-CPP-ACI-SCIENCE-BEFORE-IMPLEMENTATION-001`'s non-duplication intent.

## Next 3 concrete actions (AFTER STOP #1 approval)
1. Write CONSTITUTION.md + NON_DUPLICATION_LEDGER.md + SYSTEM_REGISTRY.md + DATASET_REGISTRY.md + DATASET_DEPENDENCY_GRAPH.md + EXISTING_SYSTEM_INTEGRATION.md (Phase 1 canonical architecture).
2. Build TIER 0 Foundations F1→F2→F3 to benchmark depth (1,145–1,357 w/Part), micro-commit each (in the OUTPUT dir, never the PP repo).
3. Build TIER 1 C1 Institutional Circulatory Fabric.

## Hard constraints
- Do NOT alter the Claude Power Pack repo. Corpus lands in the output dir only. Never `git add -A` in PP.
- Domain Contamination Audit (0 CW-Ops hits) before sealing each dataset.
- Epistemic marking on every load-bearing claim. `.md`/`.txt` only. Only SEALED = done.
- Depth floor never lowered to fit a draft. 5 Parts of real depth > 50 nominal Parts.

## Start instruction
Read this file, then `BUILD_STATUS.md`, then `MASTER_BUILD_PLAN.md`. If STOP #1 is not yet approved, present the audit and the two-path decision — do not build. If approved, execute "Next 3 concrete actions". Do not ask again; do not re-explain.
