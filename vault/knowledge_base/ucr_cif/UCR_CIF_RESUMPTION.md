# UCR-CIF Compendium — RESUMPTION

Read this file and you can continue with zero prior context. Update it after every
sealed unit, never only at the end.

## 1. Identity

Convert `Dataset Claude Power Pack Universal Construction Ratchet & Compounding
Intelligence Fabric 1.txt` (`C:\Users\User\Downloads\`, 75,350 lines / 204,153 words)
into institutional datasets for Claude Power Pack — **without re-authoring capability the
estate already owns.**

Thesis, taken from the source itself (lines 30,124–30,186): its systems are *analysis modes
over shared institutional planes*, not new silos. `DATASET IDENTITY ≠ RUNTIME PROCESS
BOUNDARY` (brief §3) — every named system gets coverage; D2A decides the *form*.

## 2. Exact state

**SEALED**

- **Phase 0 Reality Scan** — `vault/audits/ucr_cif/01_REALITY_SCAN.md`. Estate measured at
  84 modules / 26 dataset families / **266 Parts / 1,841,936 words**. Contamination
  measured as **nine discontinuous runs, ~3,900 lines (5–8 %)**, spanning 60,200–70,299;
  corpus lines available **~71,450**. Depth contract and contamination gate **inherited**
  from `sqi/CANONICAL_ONTOLOGY.md` §9 and `SQI_CONTAMINATION_AUDIT`, not re-derived.
- **Phase 1 Source Inventory — COMPLETE.** Whole corpus read in slices.
  `SOURCE_SYSTEM_INVENTORY.md` + `SOURCE_INVENTORY_FULL.json`.
  **1,394 unique concepts. 262 dataset-bearing (18 %). 336 laws. 71 top-level `system`.
  21 cross-range spine concepts.** The 18/81 ratio held constant at 648, 1,326 and 1,394
  records — a property of the corpus, not of sampling.
- **Phase 3 D2A first pass** — `02_D2A_OWNERSHIP_AUDIT.md`. **16 of 28 spine systems
  resolved.** `d2a_engine --family-file` returned 4 KEEP / 24 DEFER, every DEFER at exactly
  45 % (its plausibility floor — **DEFER = UNKNOWN, never "new"**).
- **Traps** — `03_MISSION_TRAPS.md`, four observed, appended live.

**Commits:** `828e48d` (reality scan + D2A), `a3a66a8` (inventory + traps). Both pushed.

**Coherence anchor:** `SOURCE_INVENTORY_FULL.json` must parse and hold ≥1,394 records with
a `KIND` on every one. If it does not, Phase 1 is not actually sealed.

## 3. Active decisions

- **OPEN — Owner must rule before Phase 6:** coverage form **A / B / C**, in
  `vault/plans/ucr-cif-compendium-2026-08-25.md` §10. Recommendation **A** (D2A-gated).
  B's premise was ~60–80 dataset-bearing systems needing fresh 20-Part treatments; 71
  top-level systems exist, but D2A shows most are carried by `cpp_ias` (496,679 w),
  `d2a_fabric` (303,231 w) and `clae` at 30–42k words per dataset.
- **SETTLED:** inherit the depth and contamination contracts. Follow existing vault
  conventions (`vault/knowledge_base/<family>/`). No executable code inside datasets.
- **SETTLED:** per-slice agent output — one Read + one Write per slice, each to its own
  file. Two agents died on the 64k per-response cap before this; 3-for-3 since.

## 4. Next three actions

1. **Phase 2 — KIND re-verification.** Re-check the 71 `system` and 73 `subsystem` rows
   against the source text. KIND decides whether a concept is a dataset subject at all, and
   it is currently a single agent's judgement. Note 262 is an **upper bound**: dedupe is by
   name+alias, so the same object named differently by two agents still counts twice.
2. **Phase 3b — resolve the remaining 9.** `UCR-CIF` umbrella · `HIC-OAR` whole ·
   `UDFLL` · `Constitutive Baseline Ratchet` · `Capability Authority Registry` ·
   `Knowledge Compiler/Linker` · `Meta-Failure Genome` · `Institutional SLOs & Chaos` ·
   `Long-Horizon Campaign`. **Probe the function under any name — never the acronym.**
   A vocabulary zero is UNKNOWN; that error already occurred twice here, once mine.
3. **Phase 5 — Atlas, glossary, relationship and flywheel matrices** from the inventory
   plus resolved verdicts. Typed causal edges only; "everything improves everything" is
   rejected by the brief §49.

## 5. Start instruction

Read `01_REALITY_SCAN.md` §5 (prior-art base rates) and `02_D2A_OWNERSHIP_AUDIT.md` §2–3.2
before proposing any construction. Then execute action 1. **Do not generate any dataset
until the Owner rules A/B/C.** `HR-NOVELTY-001` requires the 13-question proof against a
discovered sweep before any new institutional system is admitted; it has not been run for
the 4 KEEP candidates.
