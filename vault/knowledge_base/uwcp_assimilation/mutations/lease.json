{
  "gate": "tools/test_lease.py",
  "mutations": [
    {"name": "no-fence-bump-on-end", "file": "modules/lease/store.py",
     "old": "                    gen[c] = gen.get(c, 0) + 1\n                    if owner.get(c) == lease.lease_id:",
     "new": "                    if owner.get(c) == lease.lease_id:",
     "expect_red": ["V-LEASE-QUEUE-WAIT-EXPIRES"]},
    {"name": "no-queue-wait-expiry", "file": "modules/lease/store.py",
     "old": "            if lease.state == RESERVED and lease.queue_deadline is not None \\\n                    and now >= lease.queue_deadline:",
     "new": "            if False:",
     "expect_red": ["V-LEASE-QUEUE-WAIT-EXPIRES", "V-LEASE-QUEUE-WAIT-FREES"]},
    {"name": "no-active-expiry", "file": "modules/lease/store.py",
     "old": "            elif lease.state == ACTIVE and lease.deadline is not None and now >= lease.deadline:",
     "new": "            elif False:",
     "expect_red": ["V-LEASE-EXPIRY-FENCES", "V-LEASE-RESTART-NO-EXTEND"]},
    {"name": "ignore-busy", "file": "modules/lease/store.py",
     "old": "            busy = [c for c in classes if owner.get(c)]",
     "new": "            busy = []",
     "expect_red": ["V-LEASE-EXCLUSIVE", "V-LEASE-ALL-OR-NOTHING", "V-LEASE-RACE-ONE-WINNER"]},
    {"name": "partial-grant", "file": "modules/lease/store.py",
     "old": "            busy = [c for c in classes if owner.get(c)]",
     "new": "            busy = [c for c in classes[:1] if owner.get(c)]",
     "expect_red": ["V-LEASE-ALL-OR-NOTHING"]},
    {"name": "no-ttl-floor", "file": "modules/lease/store.py",
     "old": "ttl_s >= self.min_ttl_s):",
     "new": "ttl_s > 0):",
     "expect_red": ["V-LEASE-MIN-TTL"]},
    {"name": "ttl-from-grant", "file": "modules/lease/store.py",
     "old": "            qd = self.clock() + queue_wait_s if queue_wait_s is not None else None",
     "new": "            qd = self.clock() + min(ttl_s, queue_wait_s or ttl_s)",
     "expect_red": ["V-LEASE-TTL-FROM-DISPATCH"]},
    {"name": "no-holder-check", "file": "modules/lease/store.py",
     "old": "        if lease.holder != holder:\n            return Result(False, NOT_HOLDER, lease_id, state=lease.state)",
     "new": "        pass",
     "expect_red": ["V-LEASE-HOLDER-ONLY"]},
    {"name": "no-fence-compare-in-guard", "file": "modules/lease/store.py",
     "old": "        if dict(fences) != lease.fences:",
     "new": "        if False:",
     "expect_red": ["V-LEASE-HOLDER-ONLY"]},
    {"name": "check-accepts-any-fence", "file": "modules/lease/store.py",
     "old": "            if gen.get(cls) != fence or lease.fences.get(cls) != fence:",
     "new": "            if False:",
     "expect_red": ["V-LEASE-STALE-VS-NEW-OWNER"]},
    {"name": "lock-is-noop", "file": "modules/lease/store.py",
     "old": "        self._lock = _Exclusive(self.root / LOCKFILE, lock_timeout_s)",
     "new": "        self._lock = __import__('contextlib').nullcontext()",
     "expect_red": ["V-LEASE-LOCK-BLOCKS"]},
    {"name": "no-torn-isolation", "file": "modules/lease/store.py",
     "old": "                    prefix = b\"\\n\"          # isolate a torn tail so it can never merge",
     "new": "                    prefix = b\"\"",
     "expect_red": ["V-LEASE-TORN-TAIL"]},
    {"name": "consume-not-absorbing", "file": "modules/lease/store.py",
     "old": "                               \"queue_expire\": QUEUE_WAIT_EXCEEDED, \"consume\": CONSUMED}[op]",
     "new": "                               \"queue_expire\": QUEUE_WAIT_EXCEEDED, \"consume\": RESERVED}[op]",
     "expect_red": ["V-LEASE-CONSUME-ABSORBING"]},
    {"name": "fs-guard-off", "file": "modules/lease/store.py",
     "old": "        if why:\n            raise UnsupportedFilesystem(f\"{self.root}: {why}\")",
     "new": "        pass",
     "expect_red": ["V-LEASE-FS-CONSTRUCT"]}
  ]
}
