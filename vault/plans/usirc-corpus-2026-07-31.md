---
title: USIRC — Universal System Intelligence & Reconstruction Civilization — Phase −1 Audit
date: 2026-07-31
status: STOP #1 RESOLVED — Owner selected Option B (build the residue as one module),
  2026-07-31. Built and green the same session: modules/osr/, 30/30 V-OSR-* gates
  hermetic across 3 runs, all 5 modules REACHABLE, 0 new liveness gate offenders.
source: "Downloads/Dataset Claude Power Pack Universal System Intelligence &
  Reconstruction Civilization 1.txt" — 6,395 lines / 85,908 bytes, read to EOF
denominator: discovered this session — 1,350 files swept, 75 modules, 26 dataset
  families, 64 vault sub-surfaces, 38 hooks, 64 commands, 12 agents, 289 tools
artifacts: vault/audits/usirc/ (9 files)
novelty_gate: HR-NOVELTY-001 fired automatically on this prompt
---

# USIRC — Phase −1 Ownership and Overlap Audit

## Phases executed

- **Phase −3 Repository discovery.** Branch `main`, `REMOTE_DELTA 0 0`, working tree
  carrying a concurrent pane's untracked `vault/audits/ccfl_pdpf/`. Counts measured,
  not recalled. → `vault/audits/usirc/SYSTEM_INVENTORY.md`
- **Phase −2 Source ingestion.** Full document read to EOF in four passes. No
  credential-bearing URLs present in the file. Quality references were used for depth
  calibration only; zero commercial-domain vocabulary enters any artifact.
- **Phase −1 Ownership and overlap audit.** 89 mechanisms classified individually
  against a discovered denominator. → 9 artifacts in `vault/audits/usirc/`.

## The verdict

**0 of 13 categories, and 0 of ~160 chartered dataset slots, clear the novelty gate.**
77 of 89 mechanisms (87 %) have a verified owner. One source premise is false. Two
categories are out of scope for this repo.

**Three mechanisms survive, and one law.** They are a module, not a civilization:

| ID | What | Why it survived |
|---|---|---|
| OSR-1 | a typed model of an **external observed product** | measured zero; graphify models this repo, cpp_ias the ensemble, DAIF cognitive work, crawl_os documents — all four opened |
| OSR-2 | comparison of a **rendered build against a captured reference** | measured zero; all three of the estate's visual surfaces opened and none compares two artifacts |
| OSR-3 | **two-execution alignment** to locate the earliest divergence | measured zero; replay compares a run to a baseline, never two live traces |
| OSR-L1 | *a reached terminal state does not witness the ordering of prerequisite contracts* | the general law is owned; the ordering specialization is not |

## What the source got right, on the record

The document's own founding sentence is correct: PP holds the organs and lacks a
unified owner of the end-to-end process. It also correctly refuses several
duplications on its own initiative — it tells the reader not to replace OVO, not to
fork the replay harness, not to build a second video downloader, not to conflate the
institutional digital twin with a product twin, and to let `dataset_first` decide
whether any dataset is warranted. Where it is wrong, it is wrong in one direction: it
inspected roughly a dozen surfaces and inferred absence for everything it did not
inspect — `crawl_os` (5 sealed datasets), DAIF-03 (20 sealed Parts), CLAE (26 sealed
Parts) and ACIS were all invisible to it.

## The three options presented to the Owner

**A — Record and close.** No construction. Ship the audit, three Owner-queue rows for
the named gaps, one UKDL trap (a source's inventory of PP is a hypothesis; KADOS was
its CLASE 1 instance), one row on the base-rate table. Cost: this session only.

**B — Build the residue as one module.** `modules/osr/` with OSR-1's schema on
graphify types, OSR-2's comparison instruments registered in CLAE Part XIII, OSR-3's
alignment feeding CRAIF, plus OSR-L1 through `rule_compiler`. Five registrations
against four existing owners. Bounded by `BOUNDARY_CONTRACT.md`'s ten prohibitions.
No new family, no new graph, no second fidelity authority.

**C — B, then one dataset of doctrine for OSR-1 only**, if the Owner wants the
observed-system ontology written as institutional doctrine rather than only as code.
One dataset — never twelve, and never before the code exists.

## Standing obligations honoured by this audit

- `PR-COVERAGE-BY-CONSTRUCTION-001` — the denominator was discovered, never curated.
- `T-OWNERSHIP-AUDIT-ABSORPTION-BIAS-001` — every `MISSING` verdict was reached by
  opening the nearest incumbents, never by failing to find a word; 11 rows resting on
  a family name plus a count are marked MEDIUM.
- `feedback_zero_cannot_fall` — all 21 measured zeros were re-tested against the
  mechanism rather than the phrase; 19 resolved to a named owner under other words.
- Owner ruling 2026-07-29 — run the overlap audit against a complete, discovered
  denominator **before** any construction.

## Base rate after this audit

| Proposal | Measured | Outcome |
|---|---|---|
| AISHF | 75–80 % owned | became CRAIF |
| RE Baseline (literal A–J) | 55–60 % owned | 3 NEW + 5 EXTEND chartered |
| KSF | 70–80 % owned | reduced to a 4-family residue |
| CRPF | ~80 % owned | struck |
| IGEF | 0 of 4 mechanisms | struck |
| E1–E5 | 15 of 17 mechanisms | struck |
| UKR Compendium | 6 of 6 mega-systems owned | shelved |
| IIG (30 candidates) | 29 of 30 owned | 0 families; 1 Hard Rule |
| Emergence Runtime | owned | EXTEND_EXISTING_OWNER |
| CCFL-PDPF | 14 of 15 named systems held territory | 0 families; 4 narrow gaps |
| **USIRC** | **77 of 89 mechanisms owned** | **0 families; 3 mechanisms + 1 rule** |

Eleven proposals. One family built. The number is now large enough that it is itself
the finding: this estate's corpus is mature enough that a new mega-proposal's expected
yield is a module, and the audit that establishes this is cheaper than the corpus by
three orders of magnitude.
