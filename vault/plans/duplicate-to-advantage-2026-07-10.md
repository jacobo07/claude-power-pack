# Duplicate-to-Advantage Engine -- ULTRA-PLAN + Reality Scan (FASE 0)

> Session: DuplicateToAdvantage-Datasets-2026-07-10
> Status: **STOP #1 -- awaiting Owner scope approval. Nothing built yet.**
> Thesis: "Ninguna duplicidad termina en rechazo. Toda duplicidad se convierte en
> busqueda estructurada de la mejor capacidad adyacente que aun no existe."

---

## FASE 0 -- Reality Scan (COMPLETE, evidence-based)

Read in full: COGNITIVE_OS_INDEX (CO-00..CO-12), PARALLEL_MESH_INDEX (PM-01..PM-05),
GRAPHIFY_INDEX (GK-00..GK-12), FD_INDEX (FD-00..07), FIOS_INDEX (SCS C84),
cdio/ (CDIO-00..05), evolution_engine.py source, fable_distillation modules.

### The decisive precedent: FIOS (SCS C84, 2026-07-10, yesterday's seal)

A proposed **17-system family** collapsed at its own STOP #1 to **3 code engines + 1
doctrine index, ZERO new prose datasets** -- because "writing prose that *describes* an
execution layer would re-narrate FD/CO doctrine, the bloat GK-00 / SCS C41 /
PR-FABLE-DELTA-ONLY-001 forbid." Verdict rule established: **is the delta *knowledge*
(-> prose dataset, FD precedent) or *execution* (-> code, FIOS precedent)?**

The D2A Engine is a repeatable **decision procedure** run on an incoming architecture
proposal. Its building blocks already ship as code. Applying the D2A anti-inflation
contract (rule 8: "cuando puede ser una Part o regla, no dataset") **to D2A itself**
returns: this is mostly EXTEND of existing engines + one genuinely-new *prospective
intake* delta, NOT 6 prose datasets.

---

## D2A OVERLAP MAP (the STOP #1 deliverable)

| Component | Verdict | Parent(s) with evidence | Genuine delta (1 line) |
|---|---|---|---|
| **D2A-1 Duplicate Detection Core** | **EXTEND** (thin NEW delta) | FD-01 Delta Extraction (`NEW/STRONGER/DUP/DISCARD`, but on a frontier OUTPUT vs baseline) · `evolution_engine.merge` (title-Jaccard near-dup on EXISTING files, `_MERGE_TITLE_OVERLAP=0.6`) · PM-03 Redundancy Tax (blocks already-done WORK) · PM-02 (pane-scope collision) | *Prospective* 3-axis (semantic/functional/architectural) dup detection on a **not-yet-built proposal** -- no parent takes a proposal as input |
| **D2A-2 Capability Gap Mapper** | **EXTEND** | CO-12 Capability Gap Radar (folded -> FD-05) · GK-09 coverage/blind-spot/hotspot observatory · `evolution_engine` health signals | On-demand full capability-space map around a **specific parent**, triggered by a dup hit (14 dims) -- composes Gap Radar + GK-09 |
| **D2A-3 Vertical Reinforcement Generator** | **EXTEND `evolution_engine`** | `evolution_engine` mutation kinds `reinforce`/`abstract`/`specialize` (whole-KB proposals) | "Generate the layer BELOW the parent" prospective framing + per-candidate measurement contract |
| **D2A-4 Horizontal Reinforcement Generator** | **EXTEND `evolution_engine` + GK-04** | `evolution_engine.merge`/`abstract` · GK-04 typed cross-family edges · FD-06 Cross-System-Reinforcement (folded) | New-interface candidates connecting parent to other families + emergent-capability naming |
| **D2A-5 Alternative Portfolio Optimizer** | **EXTEND** | `token_irr.py` (`reuse_multiplier`/`compound_roi`/`payback_tokens`) · PM-04 Budget Auction (ROI rank) · CO-01 WU/MTok · FD-04 (portability verdict) | 16-dim scoring + "best ratio, not biggest" formula -- composes token_irr + PM-04 + CO-01 + FD-04 |
| **D2A-6 Reinforcement Build Governor** | **EXTEND FD-03** | FD-03 Insight Triage (routes to 8 homes: HardRule/ProcessRule/Trap/dataset-Part/benchmark/prompt-fragment/discard) · one_shot compiler · spec_gate · UKDL | Explicit 10-rule anti-inflation gate + 15-operation vocabulary (DEEPEN..RETIRE); codifies the reality-scan discipline that today lives as scattered doctrine |

### Task-flagged specific verifications

- **CO-08 (Parallel Cap) has duplicate detection?** No. PM-02 recalibrated it with
  *pane-scope* collision; PM-03 adds a Redundancy Tax on *work*. Neither detects
  duplicate **architecture proposals**. (Task's own D2A-1 note confirms this layer
  distinction is real.)
- **GK-00/GK-09 has a gap mapper?** GK-09 has coverage/blind-spot/hotspot observation
  over the graph -- gap-adjacent, but not a per-parent capability-space map triggered by
  an incoming proposal.
- **FIOS `evolution_engine.py` has mutation proposals?** YES -- 7 kinds. Covers most of
  D2A-3/4/6, but **retrospective** (scans existing KB), not **prospective** (proposal
  intake). This is the single most-overlapping parent.
- **PM-02 detects overlaps?** Pane-scope overlaps, not architecture-proposal semantics.
- **Cognitive Leak Taxonomy C70 covers Detection Core?** No -- C70 is a *non-token leak*
  taxonomy (scheduled-task health, inert bus), unrelated to duplicate detection.

### Honest bottom line

**0 of 6 components are pure-NEW prose datasets.** All 6 are EXTEND compositions of
existing engines, unified by **one genuinely-new delta no single parent owns: a
*prospective intake pipeline* that takes a not-yet-built proposal and runs
detect -> map -> generate -> score -> govern.** Building 6 x 3-Part x >2500-word prose
datasets would re-narrate evolution_engine + FD-01 + FD-03 + FD-05 + CO-12 + PM-04 +
token_irr -- the exact bloat GK-00 / SCS C41 / PR-FABLE-DELTA-ONLY-001 and the D2A
anti-inflation contract (rule 8) forbid. **The D2A Engine, applied to itself, does not
recommend building itself as 6 datasets.**

---

## Scope options for the Owner (STOP #1)

**Option A -- FIOS precedent (leanest, recommended by the anti-inflation contract):**
- `modules/duplicate_to_advantage/d2a_engine.py` -- prospective intake pipeline composing
  the 6 stages over evolution_engine / FD-01 / CO-12 / token_irr / FD-03. Input: a
  `Proposal(description)`. Output: `D2A_VERDICT` (dupe verdict + gap map + candidate
  portfolio + build contract). Owner-gated, fail-open, propose-never-apply (mirrors
  evolution_engine + LOCK-001).
- `vault/knowledge_base/duplicate_to_advantage/D2A_INDEX.md` -- doctrine index +
  crosswalk + the law + Token Budget Planner worked example.
- UKDL: PR-DUPLICATE-TO-ADVANTAGE-001 + T-D2A-ANTIINFLATION-VIOLATION-001.
- `tools/test_duplicate_to_advantage.py` -- behavior V-gates (not word-count).

**Option C -- Hybrid (recommended primary: honors the request AND the contract):**
- `D2A-00` **one** prose doctrine dataset at FD depth (3 Parts, >2500w): the paradigm,
  the 15-operation vocabulary, the 10-rule anti-inflation contract, the Token Budget
  Planner trace + its 5 generated alternatives -- this is *genuinely-new knowledge*
  (how to think about duplication as opportunity), FD-precedent-justified.
- PLUS the `d2a_engine.py` code from Option A (the *execution* delta).
- PLUS D2A_INDEX + the 2 UKDL rules + the behavior test suite.

**Option B -- Full prose family as literally specified (6 datasets x 3 Parts x >2500w):**
- Only justified if the Owner wants D2A as a standalone teachable doctrine artifact
  independent of the code. Risk: re-narrates 6 sealed engines' doctrine. The V-gate
  `V-D2A-NO-DUPLICATE` would be under real strain against evolution_engine/FD-01/FD-03.

**Recommendation:** **Option C.** It ships the one genuinely-new *doctrine* as prose
(FD-style) and the one genuinely-new *execution* delta as code (FIOS-style), obeys the
anti-inflation contract the request itself mandates, and is the most faithful possible
demonstration of the D2A thesis -- because it is D2A applied honestly to D2A.

---

## On approval -> FASE 1/2/3 (unchanged from request, scaled to chosen option)

Architecture of the 6 components (D2A-1..6), the OPERATIONS vocabulary
(DEEPEN/CONNECT/GENERALIZE/SPECIALIZE/AUTOMATE/DETERMINIZE/EVALUATE/HARDEN/COMPRESS/
PORT/COMPOSE/MUTATE/REPLACE/RETIRE/DO_NOT_BUILD), the structured output
(DUPE VERDICT / REINFORCEMENT MAP / CANDIDATE PORTFOLIO / RECOMMENDED ACTION /
BUILD CONTRACT), the Token Budget Planner worked example, the test suite
(V-D2A-* gates x3 hermetic), UKDL rules, SCS seal (next free id), and
`git push -> REMOTE_DELTA = 0 0` all follow the approved option's shape.
