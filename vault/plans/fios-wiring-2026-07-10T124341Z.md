# FIOS live-path wiring -- plan (SCS C84 addendum)

Generado 2026-07-10T124341Z. EXECUTION MODE. Respaldo del plan inline aprobado.

## Objetivo
Wirear los dos engines advisory de FIOS a su superficie viva, con el patron ya
probado por FD-00/FD-07:
- `session_compiler` -> kclaude PREFLIGHT (sesion frontier + objetivo declarado).
- `token_irr` -> STOP-chain (sesion frontier, mismo cadence-gate que FD-07).

## Hallazgos del PASO -1 (leido, no re-investigar)
- `tools/kclaude.ps1:59` YA exporta `PP_FRONTIER_SESSION='1'` en TODA invocacion
  (kclaude lanza Opus = frontier por construccion). => firing en cada launch
  escribiria un SESSION_ZERO casi vacio por launch = bloat que el propio
  evolution_engine marcaria (PR-FABLE-DELTA-ONLY-001). Gate honesto:
  **frontier + objetivo declarado** (content-check-first, disciplina pm03).
- `modules/fable_distillation/fd_07_flywheel.py` es el template exacto del Stop:
  entrada Stop-chain PY_EXE (dispatcher CHAIN_MAP linea 150), `main()` lee stdin
  JSON, gate `_is_frontier_session()` (PP_FRONTIER_SESSION), emite
  `{"systemMessage": ...}`, SIEMPRE exit 0. token_irr lo espeja -> sin glue Node.
- `hooks/pm03_publish_stop.js` = disciplina "pure-fs check first, spawn solo si hay
  trabajo, fail-open {continue:true}".
- Los import de la PREFLIGHT del prompt (`SessionCompiler`/`TokenIRR` clases) NO
  existen: los modulos exportan FUNCIONES (`compile_session`, `compute_irr`).
  Wire contra la API real (HR-PREMISE-001).

## Refinamientos honestos sobre el plan literal (documentados)
1. **PREFLIGHT gated por objetivo declarado**, no por cada launch (evita spam de
   SESSION_ZERO vacios; kclaude pone frontier=1 siempre). Fuente de la
   declaracion: `$env:PP_SESSION_OBJECTIVE` (+ `PP_SESSION_QUESTIONS`/`_BUDGET`)
   o `.pp_frontier.json` en el repo. Sin declaracion -> silencio, sin spawn python.
2. **IRR Stop gated por frontier** (igual que FD-07), no "toda sesion" -> evita un
   signal CO-12 por turno en sesiones bare. En una sesion kclaude (frontier=1) el
   /kclear SI emite "FIOS IRR:".
3. **tokens_spent** en el Stop = fuente opcional `PP_SESSION_TOKENS`, si no
   `unmeasured` (0). Las metricas model-independientes (assets, FDI, reuse,
   balance sheet) no necesitan tokens -> IRR honesto sin plumbing nuevo.

## Pasos
- W1 kclaude preflight -> `Invoke-FiosPreflight` (frontier + decl gate, ASCII-only,
  fail-open) llama `python session_compiler.py --preflight --repo <cwd>`.
- W2 Stop-chain -> `token_irr.py` gana `stop_entry()` + argv-dispatch; entrada
  PY_EXE nueva en `hook-dispatcher.js` Stop-chain.
- W3 resync -> Copy-Item canonical `hooks/hook-dispatcher.js` -> `~/.claude/hooks/`
  (Owner-side, HR-001 / T-HOOK-DISPATCHER-DRIFT-001).
- W4 tests + seal -> 6 V-gates nuevas (preflight fires/silent/failopen, irr-on-stop,
  frontier-gate, dispatcher+kclaude static-wired), x3 hermetico; UKDL
  T-FIOS-FRONTIER-SESSION-DETECT-001; SCS C84 addendum; pathspec commit; push
  REMOTE_DELTA 0 0.

## Done-gate
SESSION_ZERO generado con objetivo declarado; log muestra FIOS IRR en Stop
frontier; ambos fail-open; dispatcher en sync o Copy-Item documentado; V-gates x3;
seal; REMOTE_DELTA 0 0.
