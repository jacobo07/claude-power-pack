# D2A Gate Wiring — EXECUTION MODE (SCS C85 addendum, 2026-07-10)

> Goal: when the Owner proposes creating a new system/dataset, D2A runs FIRST and the
> Owner sees the DUPE VERDICT + BUILD CONTRACT before Claude starts building.
> Root law: `PR-DUPLICATE-TO-ADVANTAGE-001`. Scope trap: `T-D2A-GATE-KEYWORD-SCOPE-001`.

---

## PASO -1 — premises verified (HR-PREMISE-001)

The prompt's premises were **wrong** and were corrected against source before any edit:

| Premise as given | Reality (verified) |
|---|---|
| `from ... import D2AEngine` | ❌ no such class. |
| `check_proposal(text)` | ❌ no such function. |
| — | ✅ **`run(prop: Proposal) -> D2AVerdict`**; `Proposal(description, name='')`. |
| `is_duplicate` on the verdict | ✅ but nested: `verdict.dupe.is_duplicate`. |
| "PreToolUse hook … input del Owner contiene keywords" | ❌ **contradiction.** PreToolUse receives `{tool_name, tool_input}` and NEVER the prompt text. |

**Surface decision:** the gate is a **UserPromptSubmit** hook. That is the only surface
that (a) carries the Owner's prompt and (b) runs before Claude acts. Precedent already in
the chain: `prd-keyword-sentinel.js`. Behavioural contract copied from `graph_first_gate.js`
(GK-12): level-2 advisory, fail-open absolute, never exits 2. Recorded as a SURFACE NOTE in
`T-D2A-GATE-KEYWORD-SCOPE-001` so a future session does not "fix" it back to PreToolUse.

## W1 — `hooks/d2a_gate.js`

Conjunctive detection (`isCreationProposal`): **(creation verb) AND (architecture noun)
AND NOT (act-on-existing verb)**. Then `d2a_engine.py --stdin --json`; advise only if
`dupe.is_duplicate`. Throttled per (session, sha1(prompt)) 15 min. Fail-open: engine
missing / python broken / timeout / non-JSON / bad verdict -> empty stdout, exit 0.

**Engine defect found by the gate (fixed, not worked around):** `"crear un router de
modelos"` scored 34% and stayed silent — Spanish filler tokens diluted the owned-fraction
and `modelos`/`coste` never matched the English registry. Fixed in `d2a_engine.py` with
(a) creation-verb + Spanish-filler stopwords and (b) a bilingual `_ALIASES` map restricted
to terms whose English form already exists in `FAMILY_REGISTRY` (an alias lookup, not a new
vocabulary). Now: **90% covered by CO-03**. Sealed baseline re-verified 14/14 ×3 after.

## W2 — dispatcher registration

`hooks/hook-dispatcher.js` -> `'UserPromptSubmit-chain'`, after `prd-keyword-sentinel.js`,
before `jit_skill_loader.py`, `timeoutMs: 12000` (it spawns a python child).
Path `../skills/claude-power-pack/hooks/d2a_gate.js` resolves from the LIVE dispatcher's
`__dirname` (`~/.claude/hooks`) — same convention as `graph_first_gate.js`.

## W3 — global by construction

`d2a_gate.js` resolves `PP_ROOT` from `os.homedir()`, and `d2a_engine.py` resolves its own
`_PP_ROOT` + carries `FAMILY_REGISTRY` **in code** (it reads no repo-relative data). So a
proposal made in ANY repo is judged against the PP ecosystem. No per-repo config.
Proven by `V-D2A-GATE-GLOBAL` (advisory emitted with `cwd=$HOME`).

## W4 — gates (all ×3 hermetic, `test_duplicate_to_advantage.py` 21/21)

`V-D2A-GATE-FIRES` · `V-D2A-ADVISORY-VISIBLE` (additionalContext carries DUPE VERDICT +
RECOMMENDED ACTION + BUILD CONTRACT) · `V-D2A-SILENCE-ON-NOVEL` · `V-D2A-GATE-KEYWORD-SCOPE`
(4 non-creation prompts -> all silent) · `V-D2A-GATE-FAILOPEN` (non-JSON / empty /
wrong-type stdin -> exit 0, empty) · `V-D2A-GATE-GLOBAL` · `V-D2A-GATE-REGISTERED` ·
`V-D2A-BASELINE` (FD 12/12 + FIOS 13/13 green).

Test-authoring trap hit and fixed: a function-local `import json` in `main()` made `json`
local to the whole function, so an earlier `json.loads()` raised `UnboundLocalError` that
the `except` swallowed into a false ADVISORY-VISIBLE failure.

---

## ⚠️ OWNER-SIDE STEP (HR-001 — the agent cannot write under `~/.claude/`)

`hooks/d2a_gate.js` needs **no** copy (the dispatcher references it at the skills path).
Only the dispatcher itself drifts (`T-HOOK-DISPATCHER-DRIFT-001`). Run once:

```powershell
Copy-Item "$env:USERPROFILE\.claude\skills\claude-power-pack\hooks\hook-dispatcher.js" `
          "$env:USERPROFILE\.claude\hooks\hook-dispatcher.js" -Force
```

Then `/restart` (hooks cold-load at session start). Verify by typing a creation proposal —
e.g. *"crear un router de modelos que elija entre haiku, sonnet y opus"* — and confirming
the D2A advisory appears before Claude begins. Until the copy, the gate is committed and
tested but **not firing live**.
