# CGF Phase 2 — Plan Inline Completo (approved 2026-07-22)

> Institutional backup of the plan approved inline in chat, session 822edc96. Source audit:
> `OWNERSHIP_OVERLAP_AUDIT.md` (Phase -1). This file records the plan as approved; execution
> progress is tracked via commits, not by editing this file.

## Delta verification (done before this plan was finalized)

Three files changed since Phase -1 was written: `modules/duplicate_to_advantage/__init__.py`,
`vault/knowledge_base/duplicate_to_advantage/D2A_INDEX.md` (both additive, Family Sizing Mode,
irrelevant to audited verdicts), and `vault/audits/liveness_report.md` (full regeneration,
5 -> 316 components, 2026-07-22T07:00 UTC).

Two apparent contradictions traced to root cause, not taken at face value:

- `duplicate_to_advantage/d2a_engine` reads ORPHANED in the new report. Traced: `hooks/d2a_gate.js`
  invokes it via `execFileSync` on a `path.join(PP_ROOT,'modules','duplicate_to_advantage',
  'd2a_engine.py')` (3-segment). `modules/liveness/reachability.py`'s `_TOOL_SEG_RE` only catches
  the 2-segment `tools/<file>.py` case. **False orphan — scanner bug. Original "D2A = REUSE,
  WIRED" verdict stands.**
- `cascade_prevention/engine` + `dangerous_cmds` read LIVE, "reached from
  hooks/cascade_check_bash.js." Checked `~/.claude/settings.json` directly: registers ONLY
  `hook-dispatcher.js` per event. Grepped `hook-dispatcher.js` for `cascade_check_bash`: zero
  matches. `_SEED_GLOBS`/`_LIVE_SEED_GLOBS` seed every `hooks/*.js` unconditionally by
  directory+extension, without checking the dispatcher's actual `CHAIN_MAP`. **False live —
  scanner bug in the opposite direction. Original "cascade_prevention largely unwired" verdict
  stands, now proven two independent ways.**
- Bonus correction (narrows, doesn't overturn): `rollback/rollback` and `backup/backup`
  themselves are LIVE (command-invoked); only the runner backends and
  `verify_restore`/`retention`/`source_selector` are genuinely orphaned. Workstream B scope
  narrowed accordingly.

## Mode decision

| Workstream | Mode | Why |
|---|---|---|
| A — cascade_prevention wiring | EXECUTION | Root cause fully nailed; mechanical fix |
| B — Liveness/orphan closure | PLAN | ~170 orphans, case-by-case judgment |
| C — Knowledge Capture consolidation | PLAN | 3 systems, real ownership decision |
| D — Prompt Minimalism check | EXECUTION | Confirmed narrow gap |
| E — IAS-C2 Opportunity Cost | EXECUTION | Confirmed gap, existing verdict to implement |
| F — Systemic completion-integrity standard | PLAN | Meta-level, scanner-fix design decision |

ULTRA-PLAN not warranted anywhere — every scope already bounded by Phase-1 evidence.

## Workstreams A-F

See chat delivery (session 822edc96, 2026-07-22) for full per-workstream detail: identity,
ownership, evidence, consumers, risk, action, validation, done-gate, micro-commit message, for
each of A (cascade_prevention wiring), B (liveness/orphan closure, scanner-bug-fix-first then
disposition), C (knowledge capture consolidation, no fourth system), D (prompt minimalism
check), E (IAS-C2 opportunity cost), F (strengthen modules/liveness/reachability.py: generalize
`_TOOL_SEG_RE` to N-segment `modules/` paths + cross-reference `hooks/*.js` seeding against
`hook-dispatcher.js`'s real `CHAIN_MAP`).

## Naming collision / SKILLBANK

Checked against limited-exception criteria (blocks scope validation? real ambiguity in routing/
execution? small+reversible+low-blast-radius fix?). Neither `code-review`/`code_review` nor
SKILLBANK staleness met the bar this session -> both remain separate follow-ups, logged with
evidence in `OWNERSHIP_OVERLAP_AUDIT.md`.

## UKDL

Trap confirmed twice in one delta-check: "registered in a dispatcher file = wired" is false
without checking the actual chain map — in BOTH directions (a real wiring the scanner missed;
a phantom wiring the scanner invented). Process rule: before trusting any liveness/reachability
verdict for a plan, spot-check at least one LIVE and one ORPHAN claim against the actual
invocation chain, not just the report.

## Execution order

A -> F (scanner fix) -> B (disposition, post-fix) -> D -> E -> C

## Status

Approved 2026-07-22 by Owner ("y"). Execution proceeding this session.
