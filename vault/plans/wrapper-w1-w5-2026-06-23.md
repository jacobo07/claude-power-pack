# kclaude Wrapper W1-W10 — Execution Plan (backup)

**Date:** 2026-06-23. Continues from F0 (commit b144eba, spawn audit 42/42).
**Mode:** EXECUTION. Autonomous to final done-gate except **STOP #3 after W2**.

## Reality reconciliations (plan-code-is-hypothesis, SCS C28)

- **W1 thresholds:** prompt suggested 800/1500 turns + 3/8 MB. The LIVE
  `context_monitor` is the authoritative trip (16/24 MB, 6000/12000 turns,
  20/28 GB RAM — softened 2026-06-05; UKDL T-RAM-DEDUP-001). W1 **composes**
  `context_monitor.assess()`; gaming halves via `ram_guard.minecraft_active()`.
- **W2 anchor:** transcript-on-disk is the ONLY validity anchor. W2 prefers
  `pane_map.json` (disk-derived, sub-filtered, labelled) but RE-VERIFIES each
  `<sid>.jsonl` exists now; falls back to a raw disk scan if pane_map absent.
  NEVER registry/snapshot count.
- **kclaude.bat exists** (~/.claude/kclaude.bat, MC-LAZ-26 resume loop). W8
  backs it up and integrates — never clobbers.

## Work items

- **W1** `modules/wrapper/turn_counter.py` — pre-launch pressure advisory via
  context_monitor; gaming halving; fail-open None; injectable assess_fn.
- **W2** `modules/wrapper/auto_resumer.py` — transcript-anchored `--resume <sid>`;
  pane_map-primary + disk-reverify + disk-fallback; multiple → most recent + others.
  **STOP #3** here.
- **W3** `modules/wrapper/session_namer.py` — poll session_snapshot for new sid
  (10s), label = repo + git subject; reconcile with build_pane_map regeneration.
- **W4** `modules/wrapper/repo_coordinator.py` — same-repo active (<2h) detection;
  returns data, W6 prompts.
- **W5** `modules/wrapper/cost_gate.py` — compose tco_compact_gate + budget_monitor
  + classify_tier; budget gate, model suggestion, compact savings; fail-open.
- **W6** `tools/kclaude.ps1` — orchestrator: W1→W4→W5→W2→launch→W3(bg); per-feature
  timeout; overhead <2s; fail-open absolute.
- **W7** apply remaining external windowsHide (settings.json orphan-reaper).
- **W8** `tools/install_wrapper.ps1` — backup existing, copy to bin, PATH add.
- **W9** `tools/test_wrapper.py` — 11 V-gates ×3 hermetic.
- **W10** SCS C48 + UKDL (T-SPAWN-WINDOW-001, T-WRAPPER-TRANSCRIPT-ANCHOR-001,
  T-WRAPPER-TIMEOUT-001) + push.

## Contract

Fail-open absolute; transcript-on-disk único anchor; never hard-block claude;
per-feature timeout; overhead <2s; pathspec-scoped commits; no `git add -A`.
