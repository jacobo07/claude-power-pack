# PM-06 + CO-08 Intent-Gate wiring — EXECUTION report (2026-07-04)

Three planned items (W1 PM-06, W2 CO-08 scope-gate, W3 tests+seal). PASO -1 disproved two
premises → the sprint reduced to the one real gap.

## PASO -1 — INERT SYSTEMS REPORT (verified)
- **PM-06: does not exist** — parallel mesh is PM-01..PM-05. **W1 skipped** (per the prompt's
  own "si PM-06 no existe" constraint).
- **CO-08 = `modules/cognitive_os/scheduler.py`**, not `co_08_parallel_cap.py` (disproven path).
- **CO-08 intent-gate logic already built** (`scheduler.decide(declared=…, hot_scopes=…)`,
  C65/C66) + `pm_02_intent.scope_gated_admit` + `PaneIntentRegistry` + CLI + V-PM02-* tests.
  The prompt's "recalibración nunca ejecutada" was **disproven**.

## The real gap (W2) — inert architecture tax
`modules/wrapper/prelaunch._gate` — the live CO-08 launch gate kclaude acts on — called the
blunt `scheduler.admit()` and never passed `declared`. So the built intent-gate was
**unreachable at launch**; every pane hit `SAME_REPO_CAP=1` regardless of declared intent.

**Fix:** `_gate` is now intent-aware — declares a scope via `PP_PANE_SCOPE` env
(`PP_PANE_SID` excludes the pane's own intent) → scope-gated via `scope_gated_admit`; no
scope → blunt `admit()` unchanged (zero regression). Fail-open → blunt cap on any error. Added
`now`/`proj_base`/`gather_fn`/`registry` injection so the live entry point is hermetically
testable. `SAME_REPO_CAP=1` kept as the failsafe — never removed, only bypassed by a proven-
disjoint declared scope.

## W3 — tests + seal
`tools/test_parallel_mesh.py` **31/31 ×3** (+4 CO-08 gates driving the LIVE `_gate`):
`V-CO08-DIFFERENT-REPO-FREE`, `V-CO08-NOINTENT-CAPPED`, `V-CO08-INTENT-ALLOWED`,
`V-CO08-INTENT-COLLISION`. No regression: `test_cognitive_os_build` 68/68, `test_scope_a` 7/7.
Sealed **SCS C76** + UKDL `T-INERT-ARCHITECTURE-TAX-002`.

## Owner-side (documented, not executed — HR-001)
`prelaunch.py` loads from the skills repo path, so the wiring is live next launch — but the
scope-gate only *activates* when the launcher sets `PP_PANE_SCOPE`. Automatic activation = have
`kclaude.ps1` export `PP_PANE_SCOPE`/`PP_PANE_SID` from the pane's declared intent before
calling `prelaunch`. Per-launch opt-in works today: `$env:PP_PANE_SCOPE="modules/x.py"`, or
`python modules/parallel_mesh/pm_02_intent.py --sid <sid> --declare --scope …` first.

## Honest note
Both W2 (CO-08) and the prior PM-03 sprint were "built-but-inert-at-the-call-site", not
unbuilt — the recurring inert-architecture tax now formalized in `T-INERT-ARCHITECTURE-TAX-002`.
No new architecture; the value is wiring the live caller to the sealed logic.
