# Cognitive Kernel — Dataset Family — REALITY SCAN REPORT

**Mode:** ULTRA-PLAN · FASE -1 Reality Scan · STOP #1 (blocking)
**Date:** 2026-07-03
**Fundamental question:** *¿Cómo hago que Claude tenga que pensar menos?* — reasoning
reduction as infrastructure; token savings as consequence, never the goal.

---

## Verdict headline (honest)

The cognitive-kernel space is **densely covered already**. Across five sealed families —
Cognitive OS (CO-00..10), Parallel Mesh (PM-01..05), Graphify Kernel (GK-00..12), Session
Resilience (G1..G6), and the audit axis (C68 volume / C69 behavior / C70 leak taxonomy) —
plus the 23-part `pp_dataset` corpus, **every one of the four GAP candidates in the prompt
is COVERED or PARTIAL. Zero are genuine total gaps.**

Per the prompt's own honesty clause ("mejor 3 datasets profundos que 20 superficiales… el
mayor ROI puede ser activar lo ya construido"), the recommendation is **not to build a
CK-** family. The genuinely-new material collapses to **at most ONE optional dataset**
(≈50% synthesis), and the **highest ROI is activation of already-built-but-inert systems**.

---

## Existing coverage map (what was read)

| Family | Seal | Scope | Central metric / law |
|---|---|---|---|
| Cognitive OS CO-00..10 | C61/C62 | context economy, routing, memory, GC, hibernation, parallel cap, loop/subagent econ, honesty ledger | 60% context ceiling; **Work-Units / M-Tokens** |
| Parallel Mesh PM-01..05 | C65/C66 | repo shared brain, intent/collision, **findings bus**, budget auction, prefetch | *Parallel allowed, duplicate cognition forbidden* |
| Graphify GK-00..12 | C71/C72 | coordinate system, semantic identity, graph compiler, typed edges, query runtime, **route compiler + minimal context pack**, freshness, **writeback**, observatory, cross-repo, librarians, graph-first enforcement | *Navigate, do not explore* |
| Session Resilience G1..G6 | C55..C58 | editor/window/snapshot/acceptance/telemetry/power-transition recovery | Restore == Reload Window |
| Audit axis | C68/C69/C70 | volume (`token_corpus_audit`), behavior (`conversation_quality_audit`), non-token leaks (`scheduled_task_health`) | measure-before-build |
| pp_dataset I..XXIII | BL-DATASET-001 | incl. **XV Self-Safe Evolution OS**, XVI Meta-Analysis, XVII Predictive Immunity, XVIII Compounding, VI Tech-Debt | — |

---

## The four GAP candidates — adjudicated

### Candidate 1 — Cognitive Compiler → **PARTIAL / already ruled EXTEND-deferred**
- **Already exists:** CO-03 Dynamic Router (Vault→asset→deterministic→Haiku→Sonnet→Opus);
  `modules/one_shot/compiler.py` (contract compiler: scope/done-gate/budget from
  description+size); `spec_gate.classify_tier`; GK-06 Route Compiler + Minimal Context Pack;
  GK-05 Query Planner + Coordinate Resolver.
- **Decisive evidence:** the PM index (line 68) **already evaluated "Cognitive Compiler" and
  ruled it `DEFERRED (EXTEND CO-03 + one_shot)` — "one_shot/compiler.py + CO-03 + spec_gate
  already compile."** The Owner-facing verdict is on record.
- **Residual sliver:** no single *unified* pre-execution compile pass ties intention → deps →
  existing-assets → routes → model → budget → plan into one entry gate. That unification is an
  **EXTEND (wiring)**, not a new dataset.

### Candidate 2 — Intelligence Yield Engine → **PARTIAL (strongest sliver, ~50% synthesis)**
- **Already exists:** CO-01 metric = *verifiable work finished per compute cost* (WU/MTok) — an
  ROI ratio. C68 fat-tail **already classifies output**: 72% dense-unique reasoning, 22% real
  `Write` deliverables, 0% repetitive. GK-08 Writeback **already decides** what session output
  becomes a permanent graph asset (verify gate + negative-knowledge contract). GK-09 Observatory
  measures coverage/ROI. CO-05 Asset Registry stores verified reusable assets.
- **Genuine sliver:** no first-class **Intelligence Yield Ratio** (*permanent reusable assets
  created / tokens consumed*) that closes the loop CO-01 ↔ GK-08 ↔ C68 and arbitrates per-output
  "asset vs ephemeral" as a named metric. This is the **one genuinely-new, defensible idea** —
  but its implementation is mostly synthesis across three existing systems.

### Candidate 3 — Cognitive Debt Ledger → **MOSTLY COVERED (one thin sliver)**
- **Recurring traps without permanent fix** → COVERED: `pp-error-analyst` agent (fires when a
  NEVER_AGAIN recurred ≥3× without a covering Hard Rule) + `bug_to_hardrule` + UKDL Traps.
- **Repeated Owner workarounds each session** → COVERED (measured): C69 P2, freq 19,
  T-OWNER-REPEAT-INSTRUCTION-001.
- **Past waste** → COVERED: C70 leak taxonomy. **Budget violations** → CO-02 Violation Registry.
  **Tech debt catalog** → pp_dataset_06.
- **Genuine sliver:** nothing tracks *"built as temporary but still load-bearing N months
  later"* — a forward-looking temporal-debt signal. Thin; a section/tool, not a family.

### Candidate 4 — Autonomous Architecture Evolution → **PARTIAL / EXTEND**
- **Already exists:** pp_dataset_15 Self-Safe Evolution OS (feature flags/shadow/canary/kill-
  switch/rollback — the *safety* of evolving); pp_dataset_16 Meta-Analysis; pp_dataset_17
  Predictive Immunity; pp_dataset_18 Compounding; `learning-sentinel.js` → LEARNINGS_PENDING;
  `/cpp-compound` compound-learnings (extracts patterns, signal thresholds, materializes
  SKILL/HOOK/RULE/AGENT, Owner-approved); `omni-singularity` post-deploy NEVER_AGAIN injector.
- **Genuine sliver:** no proposer consumes the **C68/C69/C70 audit outputs + UKDL trap
  frequency** to emit inline improvement plans. That is an **EXTEND of compound-learnings /
  omni-singularity to a new input source**, not a new dataset family.

---

## Highest-ROI path (activation, not creation) — surfaced per prompt

1. **PM-03 Findings Bus is BUILT but INERT.** C70 proved it: bus dir absent → 0 findings ever
   published. Wiring it (SessionStart digest + Stop publish) is real, measured value. Owner-side
   (HR-001). This is the single biggest live-value item in the whole cognitive stack.
2. **hook-dispatcher canonical↔live DRIFT** (T-HOOK-DISPATCHER-DRIFT-001, recurring): git
   `hooks/hook-dispatcher.js` vs live `~/.claude/hooks/` diverge on every new hook; Owner must
   `Copy-Item`. A durable sync/verify closes a recurring reasoning-tax.
3. **GK live agents + global cross-repo coordinate store** — deferred to EXECUTION at C71/C72.
4. **CO-08 scheduler recalibration** (PM-02 scope-gate relaxation of SAME_REPO_CAP) — deferred.

---

## Recommendation to Owner (STOP #1 — approve scope)

**A. Build NOTHING new; activate the inert.** (Highest ROI, honest.) Wire PM-03, close the
hook-dispatcher drift, optionally advance GK/CO-08 EXECUTION deferrals. No CK-* family.

**B. Build ONE optional dataset — CK-01 Intelligence Yield & Cognitive Debt Ledger** — only
the two genuinely-new slivers, fused: (1) the Intelligence Yield Ratio metric closing
CO-01↔GK-08↔C68; (2) the forward-looking temporal-debt tracker (temporary-turned-permanent +
audit-driven improvement proposals feeding compound-learnings). Declares CO-01/GK-08/C68/C70 as
parents. Then activate the inert (A) as well. ~50% of it is synthesis; honest about that.

**C. Owner overrides** — build a fuller CK-* family despite duplication (not recommended;
violates the prompt's own non-duplication + depth-over-quantity constraints).

**Recommended: A, or B-then-A.** The prompt's fundamental question — *make Claude think less* —
is already answered by CO+PM+GK+G. The remaining reasoning-tax is not missing architecture; it
is **built architecture sitting inert** (PM-03) and **drift** (dispatcher). Fix those first.
