# RESUMPTION — Decision Review Kernel (DRK) corpus build

**Identity.** You are building the DRK corpus (the PP "Decision axis") in
`C:\Users\User\.claude\skills\claude-power-pack`. Thesis: authenticate a decision (record,
reversibility, blast radius, precedent, evidence-burden) *before* action and judge its *reasoning*
(not just outcome) after. It DEEPENs the shallow SDD-OS Parte V enumeration + ships the executable
Parte V never had. Owner overrode the minimal-scope recommendation → fuller corpus approved
(`vault/plans/decision-intelligence-2026-07-11.md` §9–10 = SSoT architecture + coverage matrix).

**Exact state (coherence anchor — counts must match `DRK_INDEX.md` build ledger).**
Sealed: `vault/plans/decision-intelligence-2026-07-11.md` (Reality Scan + override) ·
`vault/knowledge_base/decision_review/DRK_INDEX.md` (master index) ·
`vault/knowledge_base/decision_review/drk_00_foundations_canonical_objects.md` (~2800w, canonical
objects). Pending: DRK-01..07, SDD-OS Parte VI (`sdd_os_06_…`), `modules/decision_review/`
{decision_record.py, decision_kernel.py, accountability.py}, `tools/test_decision_review.py`, 2 UKDL
rules. **Nothing committed to git yet.**

**Active decisions (obey).**
- Depth ≥2500 real words/Part at reference density; **zero code fences in doctrine** (schemas as
  field tables). Cross-reference ACIS/D2A/CO/FIOS — **never re-narrate** them (`V-DRK-CROSS-REF-NOT-RENARRATE`).
- Verdict ontology (10), taxonomy (7 axes), routing (L0–Ln), block-narrow rule (block iff Tipo-C ∧
  <ACIS-E3) are FIXED in DRK-00 — every downstream dataset + the engine must use them verbatim.
- Contamination Audit continuous: zero CommonWealth Ops terms/metaphors/commercial logic.
- Windows: Agent dispatch SOLO in its batch, ≤2 parallel; sequential writes; pathspec-scoped commits; `NEVER git add -A`.

**Next 3 actions (imperative).**
1. Author `drk_01_review_kernel_verdict_engine.md` (the review pipeline: scope-test → classify →
   route → adversarial pass → verdict; composition contracts to arch_check/one_shot/spec_gate/d2a/
   acis/cost_collapse/owner_queue; the block-gate). Then update the INDEX ledger.
2. Delegate DRK-02..06 to subagents (Agent SOLO, ≤2 parallel, overshoot+self-verify ≥2500w,
   Bash-avoidance directive); verify each against the depth + cross-ref + no-CW gates; pathspec-commit in batches.
3. Author SDD-OS Parte VI (accountability/attribution — Owner-critical) + DRK-07 (governance/authority
   limits); then build the 3 code modules + `tools/test_decision_review.py`; seal UKDL
   (`PR-DECISION-AUTHORITY-LIMITS-001`, `T-DECISION-AUTHORITY-CAPTURE-001`); push → REMOTE_DELTA 0 0.

**Start instruction.** Read this file, then `DRK_INDEX.md`, then the plan §10 architecture, then
execute action 1. Do not ask, do not re-plan, do not regenerate DRK-00. Update the INDEX ledger after every sealed unit.
