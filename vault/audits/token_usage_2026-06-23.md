# Token Usage -- Real Ground Truth

**Generated:** 2026-06-23 13:56  
**Source:** `~/.claude/projects/*/*.jsonl` `message.usage` (real per-turn, no estimates)  
**Plan:** Claude Max (flat rate) -- $ figures would be hypothetical; the real metric is cache ratio + output volume.  
**Transcripts:** 532 with usage / 545 total

## Aggregates

| Window | Input | Output | Cache reads | Cache create | Cache ratio |
|---|---|---|---|---|---|
| Today (2026-06-23) | 9,608,371 | 49,494,178 | 6,265,223,664 | 217,812,486 | 96.5% |
| Month (2026-06) | 50,529,435 | 387,543,694 | 52,833,358,915 | 1,909,073,644 | 96.4% |
| Lifetime | 52,004,411 | 448,906,808 | 63,745,243,028 | 2,215,642,278 | 96.6% |

**Cache health:** HEALTHY (threshold 50%).

## Top 15 sessions by billable (input+output)

| Billable | Date | Project | sid | Turns | Cache% | Avg fresh in/turn |
|---|---|---|---|---|---|---|
| 14,971,711 | 2026-05-26 | C--Users-User-Desktop-Cursor-P | c1008639 | 12160 | 98.2% | 15 |
| 13,712,781 | 2026-06-02 | C--Users-User-Desktop-Cursor-P | e21e3691 | 8183 | 96.9% | 49 |
| 13,711,895 | 2026-06-02 | C--Users-User-Desktop-Cursor-P | d6749b93 | 7412 | 96.6% | 8 |
| 13,588,861 | 2026-06-04 | C--Users-User--claude-skills-c | 3f8d60d4 | 7086 | 96.1% | 26 |
| 13,588,861 | 2026-06-04 | C--Users-User-Apps-mcp-video-a | 3f8d60d4 | 7086 | 96.1% | 26 |
| 12,185,418 | 2026-06-03 | C--Users-User-Desktop-Cursor-P | 43773144 | 5131 | 96.7% | 108 |
| 11,864,918 | 2026-06-06 | C--Users-User-Desktop-Cursor-P | b5293708 | 2838 | 93.3% | 283 |
| 10,858,508 | 2026-06-06 | C--Users-User-Desktop-Cursor-P | 56b87206 | 6311 | 97.9% | 76 |
| 10,038,343 | 2026-06-05 | C--Users-User-Desktop-Cursor-P | e4f02b4b | 4890 | 95.5% | 273 |
| 9,574,140 | 2026-05-31 | C--Users-User-Desktop-Cursor-P | 0220b268 | 4785 | 98.2% | 23 |
| 9,093,770 | 2026-06-23 | C--Users-User-Desktop-Cursor-P | 7cbc8cc3 | 4402 | 98.0% | 375 |
| 8,967,545 | 2026-06-05 | C--Users-User-Desktop-Cursor-P | 92b2893e | 3768 | 96.0% | 176 |
| 8,486,991 | 2026-06-20 | C--Users-User-Desktop-Cursor-P | 7f630fcf | 3125 | 97.1% | 372 |
| 8,174,855 | 2026-06-23 | C--Users-User-Desktop-Cursor-P | cd4822bc | 3014 | 96.5% | 327 |
| 7,446,099 | 2026-06-12 | C--Users-User-Desktop-Cursor-P | bf308023 | 4063 | 98.5% | 238 |

## High-consumer sessions (avg fresh input/turn > 100,000)

None. No session pressures the live context window on a per-turn basis -- cache absorbs the bulk.

## TCO System Health & Gaps (FASE -1 audit findings, 2026-06-23)

This section is hand-authored (the table above is regenerated by the tool;
these findings are the point-in-time audit and are NOT regenerated).

### What each tracking system actually measures

| System | Fires | Measures | Verdict |
|---|---|---|---|
| transcripts `.jsonl` | always (Claude Code native) | real per-turn `message.usage` | GROUND TRUTH (this report) |
| `tools/token_ground_truth.py` | on-demand (this tool) | aggregates the transcripts | NEW -- closes the no-aggregated-real-tracker gap |
| `modules/token-optimizer/token_autopsy.py` | on-demand | per-session transcript burn report | correct reader; latent `NameError` at line 409 (`mass_ops`) FIXED this audit |
| `tools/tis.py` (TIS) | every UserPromptSubmit (piggybacks `jit_skill_loader` via hook-dispatcher) | **JIT-injection size** (`model="claude-code-hook"`, cache fields always 0) | GAP: records injection overhead, not real model turns |
| `tools/budget_monitor.py` | not scheduled (absent from settings.json) | programmatic-credit runway from `jit_usage_*`/`rtk_*` | doubly inert: `~/.claude/budget.json` ABSENT + pricing 35d stale; HONEST for flat-rate Max with no programmatic spend |

### Core gap (T-TCO-TRACKING-GAP-001)
The PP's dedicated logger (TIS) writes daily continuously but logs the size of
the JIT context it injects -- not the turn's real `input/output/cache` usage.
The real billions of cache-read tokens live only in the transcripts and were,
until this tool, only readable on-demand per-session. There was no aggregated
real-usage tracker. `token_ground_truth.py` is that tracker.

### Cache ratio (A3) -- 96.6% lifetime, HEALTHY
Far above the 50% underuse line. The prompt cache absorbs ~96% of all
input-side tokens. The "ratio < 30% = variable prompts" optimization
(T-CACHE-RATIO-LOW-001) does NOT apply to this stack today. No action.

### High consumers (C1) -- ZERO
No session exceeds 100k average fresh input/turn. Top sessions are long
(up to 12k turns) but cache-efficient (93-98%); average fresh input/turn is
8-375 tokens. Nothing to /compact on a per-turn-pressure basis. The existing
context_monitor thresholds (16/24 MB jsonl, 6000/12000 turns) remain the right
gate for whole-session size.

### trace-emitter / session-logger (B1/B2) -- DO NOT EXIST
The hypothesized `trace-emitter.js` / `session-logger.js` token-capturing hooks
are not present in the PP. The only PP token logger is TIS (above). No gap to
fix beyond documenting that real usage = transcripts.

### budget_monitor (A2) -- different bucket, not a defect
budget_monitor measures the post-2026-06-15 programmatic metered-credit bucket
(Agent SDK / `claude -p`), which is independent of the Max subscription session
burn. It cannot "match" the transcript numbers because it tracks a different
thing. For a flat-rate Max Owner with no programmatic workloads its
`unconfigured` / `stale-pricing` sentinels are correct, not broken.

### C2 -- design note for the PAUSED wrapper W5 (cost_gate)
When wrapper W5 resumes (currently paused at STOP #3), its `cost_gate` MUST
source real session burn from the transcripts (`token_ground_truth.analyze()`
or `token_autopsy`), NOT from `budget_monitor` (programmatic bucket) and NOT
from TIS (injection size). Using either of the latter would show a budget gate
on data unrelated to the Owner's actual session usage.

### Bottom line
Tracking is HEALTHY where it counts (Claude Code records everything; cache 96%;
no per-turn hogs). The one real gap was observability: the PP had no aggregated
real-usage view. That gap is now closed by `token_ground_truth.py`. On Claude
Max the economic TCO is flat ($0 marginal/token); the efficiency TCO is strong.

---
*Generated by tools/token_ground_truth.py (Claude Power Pack -- TCO ground truth).*
