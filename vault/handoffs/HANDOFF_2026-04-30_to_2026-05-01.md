# Handoff — 2026-04-30 → 2026-05-01

**Read this file first** in the next session. It defines what's already
shipped, what's queued, and the live state of every running system.

## Repo state (origin/main)

7 commits today, all pushed:

```
bcb1909  chore(ledger): BL-0008 — probe-before-repair (MC-SYS-09 post-mortem)
79b031b  feat(topology+ledger): MC-OPT-18 + MC-LAZ-42 + MC-OVO-460 — on-logon hook + BL-0007
a35bc0d  feat(topology): MC-LAZ-41-MAP — 5/5 macro slot resolution + BL-0006
2dffb0d  feat(topology): MC-LAZ-40 + MC-OPT-16 + MC-OVO-440 — 5-slot Cursor topology
09caf39  feat(opt): MC-OPT-14 — pre-compact vault dump + Hardware-Law ledger
ba4693c  feat(skills): MC-SA-* — plugin scanner + 4-source merge
(plus this commit pending: MC-OPT-19 Universal Vault doc + BL-0009)
```

## Hardware-Law ledger (sealed, citable)

```
BL-0001  Plugin scanner enumerates <plugin>/skills/SKILL.md
BL-0002  Audit merger INPUTS before merge logic
BL-0003  Hooks cannot dispatch /compact; vault dump fires async only
BL-0004  /compact must be preceded by pre_compact_vault_dump.py
BL-0005  Workspace topology immutable; topology_apply.py gate
BL-0006  MC-LAZ-41-MAP: 5/5 slots resolved + label-vs-folder drift documented
BL-0007  Topology label-vs-folder duality canonical
BL-0008  Probe-before-repair: live-probe state BEFORE destructive action
BL-0009  Agent-teams orchestration is advisory-injection, not magical dispatch
```

## Live system state (ground-truth probe receipts)

| System | State | Probe |
|---|---|---|
| Docker Desktop | ✅ Engine running 4.71.0 | User screenshot 2026-04-30 |
| Auto-browser stack | ✅ ONLINE 127.0.0.1:8000 + VNC 6080 | `node lib/adapters/auto_browser.js readiness` returned `ok: true` |
| VirtualMachinePlatform | ⚠ Disabled — irrelevant (Docker uses WSL2 backend) | `dism final state Disabled` (last attempt) |
| 5-slot Cursor topology | ✅ All 5 paths resolved | `topology_apply.py --dry-run` 5/5, 0 unresolved |
| Skill index | ✅ 1251 merged, 251 collisions cataloged | `skills_index_unified.json` totals |

## Stale markers on Desktop (recommend cleanup)

```powershell
Remove-Item "$env:USERPROFILE\Desktop\.windows_repair_PENDING" -Force
Remove-Item "$env:USERPROFILE\Desktop\.vmp_fod_repair_FAILED" -ErrorAction SilentlyContinue
schtasks /Delete /TN "LazarusResume" /F
```

## Queued for next session — explicit MC list

### CP-005 — Skill installation (6 mydeepchat-only skills)

For each of these, the next session must:
1. Locate the source prompt in `vault/audits/mydeepchat_skills_raw.jsonl` (search by `name:` field)
2. Extract the prompt text
3. Write `~/.claude/skills/<name>/SKILL.md` with YAML frontmatter `name`, `description`
4. Smoke-test via `Skill` tool invocation in the next session
5. If applicable, wire into a hook (e.g., adversarial-longevity-style supremacy gate)

Skills queued:
- `ascii-video` (viral marketing)
- `content-engine` (multi-platform content)
- `ui-demo` (human movement / pacing)
- `blueprint` (anti-monolith planner)
- `gstack-openclaw-ceo-review` (max-ROI filter)
- `agent-sort` (skill bundle cleanup)

Estimated: ~30 min/skill = 3 hours of focused work in fresh session.

### CP-006 — Always-On Orchestrator hook

- New file: `~/.claude/hooks/multi-edit-orchestrator.js` (PreToolUse on Edit/Write)
- When N≥3 file mutations within a session window → inject additionalContext: "Consider `superpowers:dispatching-parallel-agents` for independent tasks."
- Honor BL-0009: this is **advisory injection**, not forced dispatch.
- Wire via `~/.claude/settings.json` PreToolUse matcher.

### CP-007 — Snowball baseline templates

- Update `parts/sleepy/agent-governance.md` to reference BL-0001..BL-0009 as the floor.
- New project bootstrap in `tools/customclaw_bootstrap.py` (or extend existing `customclaw`) should auto-cite BL-0008 as the "every project starts here" anchor.

## Pre-compact snapshot pointer

Latest sleepy index: `vault/sleepy/sleepy_index_2026-04-30T17-16-45Z.json`
(head=`bcb1909f`, dirty=43, reason=`pre-compact-clean-handoff`).

After `/compact` lands, next session reads `vault/sleepy/INDEX.md` last
line and follows the pointer to recover this state.

— end of handoff —
